import { notFound } from 'next/navigation'; import Link from 'next/link'; import data from '../../content/es.json';
const pages:any=data.pages; const routes=['regeneracion','historia','masoneria','trabajo','biblioteca','conocernos','faq'];
export function generateStaticParams(){return routes.map(slug=>({slug}))}
export default function Page({params}:{params:{slug:string}}){const p=pages[params.slug];if(!p)notFound();return <main className="page"><div className="wrap"><div className="kicker">{p.label}</div><h1>{p.title}</h1><p className="intro">{p.body}</p>{p.pending&&<div className="notice">Contenido pendiente de confirmación y autorización. Esta sección no se completa con información no verificada.</div>}{p.facts&&<div className="facts">{p.facts.map((x:string[])=> <div className="fact" key={x[0]}><b>{x[0]}</b><span>{x[1]}</span></div>)}</div>}{p.items&&<div className="items">{p.items.map((x:string[])=> <article className="item" key={x[0]}><h3>{x[0]}</h3><p>{x[1]}</p></article>)}</div>}{params.slug==='conocernos'&&<div className="actions"><Link className="button" href="/contacto">Escribir a la Logia →</Link></div>}</div></main>}
