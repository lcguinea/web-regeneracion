'use client'; import {useState} from 'react'; import data from '../../content/es.json';
export default function Contact(){const [sent,setSent]=useState(false);return <main className="page"><div className="wrap"><div className="kicker">{data.contact.label}</div><h1>{data.contact.title}</h1><p className="intro">{data.contact.body}</p>{sent?<div className="success">Gracias por tu mensaje. La confirmación es simulada en esta versión; no se ha enviado ningún correo real.</div>:<form className="form" onSubmit={e=>{e.preventDefault();setSent(true)}}><label>Motivo de contacto<select required><option value="">Selecciona una opción</option><option>Quiero conocer la Masonería</option><option>Soy masón y quiero visitar</option><option>Consulta general</option></select></label><label>Nombre<input required name="name"/></label><label>Correo electrónico<input required type="email" name="email"/></label><label>Teléfono<input required name="phone"/></label><label>Ciudad de residencia<input required name="city"/></label><label>Mensaje<textarea name="message"/></label><button className="button" type="submit">Enviar consulta</button><small>Solo solicitamos los datos necesarios para responder a tu consulta. No adjuntes documentación.</small></form>}</div></main>}
